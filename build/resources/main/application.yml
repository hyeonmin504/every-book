spring:
  messages:
    basename: messages,errors
  datasource:
    url: jdbc:mysql://mydb.c386s8m62hr2.ap-northeast-2.rds.amazonaws.com:3306/service
    username: root
    password: 12341234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl_auto: create
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
    org:
      springframework:
        orm:
          jpa: DEBUG
        transaction: DEBUG